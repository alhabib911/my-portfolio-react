import React from 'react';
import Profile from './Profile';
import Navbar from './Navbar'

const Header = () => {
    return (
        <div>
            <Navbar></Navbar>
            <Profile></Profile>
            
        </div>
    );
};

export default Header;